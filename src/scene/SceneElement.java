package scene;

import gameplay.Direction;

/**
 * A scene element in a scene case
 */
public interface SceneElement {

    /**
     * Return the scene element in a scene case
     * @return a object type of Direction
     */
    Direction getSceneElement();
}
